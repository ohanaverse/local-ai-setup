name: shell-ci

on:
  push:
    branches: [main]
    paths:
      - "bin/**"
      - "benchmarks/**"
      - "docs/**"
      - "Makefile"
      - ".github/workflows/shell-ci.yml"
  pull_request:
    paths:
      - "bin/**"
      - "benchmarks/**"
      - "docs/**"
      - "Makefile"
      - ".github/workflows/shell-ci.yml"

jobs:
  lint:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - run: make lint
